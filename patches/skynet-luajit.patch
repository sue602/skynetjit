diff --git a/skynet-src/socket_poll.h b/skynet-src/socket_poll.h
--- a/skynet-src/socket_poll.h
+++ b/skynet-src/socket_poll.h
@@ -22,6 +22,8 @@ static int sp_wait(poll_fd, struct event *e, int max);
 static void sp_nonblocking(int sock);
 
-#ifdef __linux__
+#if defined(SKYNETJIT_USE_IO_URING)
+#include "socket_uring.h"
+#elif defined(__linux__) || defined(_WIN32)
 #include "socket_epoll.h"
 #endif
 
diff --git a/lualib/loader.lua b/lualib/loader.lua
--- a/lualib/loader.lua
+++ b/lualib/loader.lua
@@ -27,6 +27,8 @@ end
 LUA_SERVICE = nil
 package.path , LUA_PATH = LUA_PATH, nil
 package.cpath , LUA_CPATH = LUA_CPATH, nil
+
+require "skynetjit.compat"
 
 local service_path = string.match(pattern, "(.*/)[^/?]+$")
 
diff --git a/lualib/skynet/db/mysql.lua b/lualib/skynet/db/mysql.lua
--- a/lualib/skynet/db/mysql.lua
+++ b/lualib/skynet/db/mysql.lua
@@ -578,7 +578,7 @@ local function _compose_stmt_execute(self, stmt, cursor_type, args)
         local types_buf = ""
         local values_buf = ""
         --生成NULL位图
-        local null_count = (arg_num + 7) // 8
+        local null_count = math.floor((arg_num + 7) / 8)
         local null_map = ""
         local field_index = 1
         for i = 1, null_count do
@@ -892,6 +892,6 @@ local function _parse_row_data_binary(data, cols, compact)
     local ncols = #cols
     -- 空位图,前两个bit系统保留 (列数量 + 7 + 2) / 8
-    local null_count = (ncols + 9) // 8
+    local null_count = math.floor((ncols + 9) / 8)
     local pos = 2 + null_count
     local value
 
diff --git a/service/debug_console.lua b/service/debug_console.lua
--- a/service/debug_console.lua
+++ b/service/debug_console.lua
@@ -440,9 +440,9 @@ local function task(session, address)
 		if t < 6000 then
 			return tostring(t/100) .. "s"
 		end
-		local hour = t // (100*60*60)
+		local hour = math.floor(t / (100*60*60))
 		t = t - hour * 100 * 60 * 60
-		local min = t // (100*60)
+		local min = math.floor(t / (100*60))
 		t = t - min * 100 * 60
 		local sec = t / 100
 		return string.format("%s%d:%.2gs",hour == 0 and "" or (hour .. ":"),min,sec)
diff --git a/lualib/skynet/service.lua b/lualib/skynet/service.lua
--- a/lualib/skynet/service.lua
+++ b/lualib/skynet/service.lua
@@ -9,6 +9,9 @@ local function get_provider()
 end
 
 local function check(func)
+	if _VERSION == "Lua 5.1" then
+		return -- LuaJIT uses a function environment instead of an _ENV upvalue.
+	end
 	local info = debug.getinfo(func, "u")
 	assert(info.nups == 1)
 	assert(debug.getupvalue(func,1) == "_ENV")
diff --git a/lualib-src/lua-crypt.c b/lualib-src/lua-crypt.c
--- a/lualib-src/lua-crypt.c
+++ b/lualib-src/lua-crypt.c
@@ -1019,13 +1019,12 @@ lxor_str(lua_State *L) {
 	if (len2 == 0) {
 		return luaL_error(L, "Can't xor empty string");
 	}
-	luaL_Buffer b;
-	char * buffer = luaL_buffinitsize(L, &b, len1);
+	char * buffer = (char *)lua_newuserdata(L, len1);
 	int i;
 	for (i=0;i<len1;i++) {
 		buffer[i] = s1[i] ^ s2[i % len2];
 	}
-	luaL_addsize(&b, len1);
-	luaL_pushresult(&b);
+	lua_pushlstring(L, buffer, len1);
+	lua_remove(L, -2);
 	return 1;
 }
diff --git a/skynet-src/socket_server.c b/skynet-src/socket_server.c
--- a/skynet-src/socket_server.c
+++ b/skynet-src/socket_server.c
@@ -111,6 +111,16 @@ struct socket {
 	int dw_offset;
 	const void * dw_buffer;
 	size_t dw_size;
+#ifdef SKYNETJIT_USE_IO_URING
+	struct skynetjit_uring_op *uring_high_head;
+	struct skynetjit_uring_op *uring_high_tail;
+	struct skynetjit_uring_op *uring_low_head;
+	struct skynetjit_uring_op *uring_low_tail;
+	struct skynetjit_uring_op *uring_write_active;
+	bool uring_read_pending;
+	bool uring_accept_pending;
+	bool uring_connect_pending;
+#endif
 };
 
 struct socket_server {
@@ -499,6 +509,9 @@ force_close(struct socket_server *ss, struct socket *s, struct socket_lock *l, s
 		return;
 	}
 	assert(type != SOCKET_TYPE_RESERVE);
+#ifdef SKYNETJIT_USE_IO_URING
+	skynetjit_uring_forget_socket(s);
+#endif
 	free_wb_list(ss,&s->high);
 	free_wb_list(ss,&s->low);
 	sp_del(ss->event_fd, s->fd);
@@ -591,6 +604,16 @@ new_fd(struct socket_server *ss, int id, int fd, int protocol, uintptr_t opaque,
 	check_wb_list(&s->low);
 	s->dw_buffer = NULL;
 	s->dw_size = 0;
+#ifdef SKYNETJIT_USE_IO_URING
+	s->uring_high_head = NULL;
+	s->uring_high_tail = NULL;
+	s->uring_low_head = NULL;
+	s->uring_low_tail = NULL;
+	s->uring_write_active = NULL;
+	s->uring_read_pending = false;
+	s->uring_accept_pending = false;
+	s->uring_connect_pending = false;
+#endif
 	memset(&s->stat, 0, sizeof(s->stat));
 	if (enable_read(ss, s, reading)) {
 		ATOM_STORE(&s->type , SOCKET_TYPE_INVALID);
@@ -644,12 +667,16 @@ open_socket(struct socket_server *ss, struct request_open * request, struct sock
 		}
 		socket_keepalive(sock);
 		sp_nonblocking(sock);
+#ifdef SKYNETJIT_USE_IO_URING
+		status = -1;
+#else
 		status = connect( sock, ai_ptr->ai_addr, ai_ptr->ai_addrlen);
 		if ( status != 0 && errno != EINPROGRESS) {
 			close(sock);
 			sock = -1;
 			continue;
 		}
+#endif
 		break;
 	}
 
@@ -663,6 +690,15 @@ open_socket(struct socket_server *ss, struct request_open * request, struct sock
 		result->data = "reach skynet socket number limit";
 		goto _failed;
 	}
+#ifdef SKYNETJIT_USE_IO_URING
+	if (skynetjit_uring_begin_connect(ss, ns, ai_ptr->ai_addr, ai_ptr->ai_addrlen)) {
+		result->data = "io_uring connect submission failed";
+		goto _failed;
+	}
+	ATOM_STORE(&ns->type , SOCKET_TYPE_CONNECTING);
+	freeaddrinfo(ai_list);
+	return -1;
+#endif
 
 	if(status == 0) {
 		ATOM_STORE(&ns->type , SOCKET_TYPE_CONNECTED);
@@ -1020,6 +1056,11 @@ trigger_write(struct socket_server *ss, struct request_send * request, struct so
  */
 static int
 send_socket(struct socket_server *ss, struct request_send * request, struct socket_message *result, int priority, const uint8_t *udp_address) {
+
+#ifdef SKYNETJIT_USE_IO_URING
+	struct socket *s = &ss->slot[HASH_ID(request->id)];
+	return skynetjit_uring_enqueue_send(ss, s, request, result, priority, udp_address);
+#else
 	int id = request->id;
 	struct socket * s = &ss->slot[HASH_ID(id)];
 	struct send_object so;
@@ -1088,6 +1129,7 @@ send_socket(struct socket_server *ss, struct request_send * request, struct sock
 		return SOCKET_WARNING;
 	}
 	return -1;
+#endif
 }
 
 static int
@@ -1134,6 +1176,10 @@ _failed:
 
 static inline int
 nomore_sending_data(struct socket *s) {
+#ifdef SKYNETJIT_USE_IO_URING
+	if (skynetjit_uring_has_pending_write(s))
+		return 0;
+#endif
 	return (send_buffer_empty(s) && s->dw_buffer == NULL && (ATOM_LOAD(&s->sending) & 0xffff) == 0)
 		|| (ATOM_LOAD(&s->type) == SOCKET_TYPE_HALFCLOSE_WRITE);
 }
@@ -1202,6 +1248,9 @@ bind_socket(struct socket_server *ss, struct request_bind *request, struct socke
 	}
 	sp_nonblocking(request->fd);
 	ATOM_STORE(&s->type , SOCKET_TYPE_BIND);
+#ifdef SKYNETJIT_USE_IO_URING
+	(void)skynetjit_uring_begin_read(ss, s);
+#endif
 	result->data = "binding";
 	return SOCKET_OPEN;
 }
@@ -1233,11 +1282,21 @@ resume_socket(struct socket_server *ss, struct request_resumepause *request, str
 	if (type == SOCKET_TYPE_PACCEPT || type == SOCKET_TYPE_PLISTEN) {
 		ATOM_STORE(&s->type , (type == SOCKET_TYPE_PACCEPT) ? SOCKET_TYPE_CONNECTED : SOCKET_TYPE_LISTEN);
 		s->opaque = request->opaque;
+#ifdef SKYNETJIT_USE_IO_URING
+		if (skynetjit_uring_begin_read(ss, s)) {
+			return report_error(s, result, "io_uring read submission failed");
+		}
+#endif
 		result->data = "start";
 		return SOCKET_OPEN;
 	} else if (type == SOCKET_TYPE_CONNECTED) {
 		// todo: maybe we should send a message SOCKET_TRANSFER to s->opaque
 		s->opaque = request->opaque;
+#ifdef SKYNETJIT_USE_IO_URING
+		if (skynetjit_uring_begin_read(ss, s)) {
+			return report_error(s, result, "io_uring read submission failed");
+		}
+#endif
 		result->data = "transfer";
 		return SOCKET_OPEN;
 	}
@@ -1315,6 +1374,9 @@ add_udp_socket(struct socket_server *ss, struct request_udp *udp) {
 		return;
 	}
 	ATOM_STORE(&ns->type , SOCKET_TYPE_CONNECTED);
+#ifdef SKYNETJIT_USE_IO_URING
+	(void)skynetjit_uring_begin_read(ss, ns);
+#endif
 	memset(ns->p.udp_address, 0, sizeof(ns->p.udp_address));
 }
 
@@ -1360,6 +1422,9 @@ dial_udp_socket(struct socket_server *ss, struct request_dial_udp *request, stru
 	ATOM_STORE(&ns->type , SOCKET_TYPE_CONNECTED);
 
 	ATOM_FDEC(&ns->udpconnecting);
+#ifdef SKYNETJIT_USE_IO_URING
+	(void)skynetjit_uring_begin_read(ss, ns);
+#endif
 	return -1;
 }
 
@@ -1706,6 +1771,9 @@ clear_closed_event(struct socket_server *ss, struct socket_message * result, int
 // return type
 int
 socket_server_poll(struct socket_server *ss, struct socket_message * result, int * more) {
+#ifdef SKYNETJIT_USE_IO_URING
+	return skynetjit_uring_poll(ss, result, more);
+#else
 	for (;;) {
 		if (ss->checkctrl) {
 			if (has_cmd(ss)) {
@@ -1817,6 +1885,7 @@ socket_server_poll(struct socket_server *ss, struct socket_message * result, int
 			break;
 		}
 	}
+#endif
 }
 
 static void
@@ -1880,6 +1949,9 @@ can_direct_write(struct socket *s, int id) {
 // return -1 when error, 0 when success
 int
 socket_server_send(struct socket_server *ss, struct socket_sendbuffer *buf) {
+#ifdef SKYNETJIT_USE_IO_URING
+	return skynetjit_uring_send(ss, buf, PRIORITY_HIGH);
+#else
 	int id = buf->id;
 	struct socket * s = &ss->slot[HASH_ID(id)];
 	if (socket_invalid(s, id) || s->closing) {
@@ -1950,11 +2022,15 @@ socket_server_send(struct socket_server *ss, struct socket_sendbuffer *buf) {
 
 	send_request(ss, &request, 'D', sizeof(request.u.send));
 	return 0;
+#endif
 }
 
 // return -1 when error, 0 when success
 int
 socket_server_send_lowpriority(struct socket_server *ss, struct socket_sendbuffer *buf) {
+#ifdef SKYNETJIT_USE_IO_URING
+	return skynetjit_uring_send(ss, buf, PRIORITY_LOW);
+#else
 	int id = buf->id;
 
 	struct socket * s = &ss->slot[HASH_ID(id)];
@@ -1972,6 +2048,7 @@ socket_server_send_lowpriority(struct socket_server *ss, struct socket_sendbuffe
 
 	send_request(ss, &request, 'P', sizeof(request.u.send));
 	return 0;
+#endif
 }
 
 void
@@ -2260,5 +2337,27 @@ socket_server_udp_send(struct socket_server *ss, const struct socket_udp_address
 int
 socket_server_udp_send(struct socket_server *ss, const struct socket_udp_address *addr, struct socket_sendbuffer *buf) {
+#ifdef SKYNETJIT_USE_IO_URING
+	int id = buf->id;
+	struct socket *s = &ss->slot[HASH_ID(id)];
+	if (socket_invalid(s, id) || s->closing) {
+		free_buffer(ss, buf);
+		return -1;
+	}
+	const uint8_t *udp_address = (const uint8_t *)addr;
+	int addrsz = udp_address[0] == PROTOCOL_UDP ? 1 + 2 + 4 :
+		(udp_address[0] == PROTOCOL_UDPv6 ? 1 + 2 + 16 : 0);
+	if (addrsz == 0) {
+		free_buffer(ss, buf);
+		return -1;
+	}
+	struct request_package request;
+	request_init(&request);
+	request.u.send_udp.send.id = id;
+	request.u.send_udp.send.buffer = clone_buffer(buf, &request.u.send_udp.send.sz);
+	memcpy(request.u.send_udp.address, udp_address, addrsz);
+	send_request(ss, &request, 'A', sizeof(request.u.send_udp.send) + addrsz);
+	return 0;
+#else
 	int id = buf->id;
 	struct socket * s = &ss->slot[HASH_ID(id)];
 	if (socket_invalid(s, id)) {
@@ -2315,4 +2417,5 @@ socket_server_udp_send(struct socket_server *ss, const struct socket_udp_address
 	send_request(ss, &request, 'A', sizeof(request.u.send_udp.send)+addrsz);
 	return 0;
+#endif
 }
 
@@ -2473,3 +2550,7 @@ socket_server_info(struct socket_server *ss) {
 	}
 	return si;
 }
+
+#ifdef SKYNETJIT_USE_IO_URING
+#include "socket_uring.inc"
+#endif
